# Block-Structure
This is a Block Structure how a block look like. It consists of a Block Id,Nonce(It is a random number that is generated ) and the data, the previous hash is the address of that block and the current hash of that block. The hash is generated by the SHA-256.
